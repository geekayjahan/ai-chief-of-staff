---
description: Run the Ambiguity Audit against a draft spec — flags undefined entities, unspecified actions, and unbounded qualifiers. Produces app/ambiguity-audit.html.
---

Invoke the `ambiguity-audit` skill.

Default input: `app/spec.html`. If the user passed a different path as an argument, use that instead.

Follow the procedure in [`modules/s1-working-backwards/ambiguity-audit-blueprint.md`](../../modules/s1-working-backwards/ambiguity-audit-blueprint.md) verbatim. Honor every halt condition.
