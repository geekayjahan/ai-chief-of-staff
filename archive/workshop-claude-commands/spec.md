---
description: Interview until the problem is sharp, then write the spec — work backwards from the customer. Produces app/spec.html, app/CLAUDE.md, app/spec-summary.json, and app/evals.md.
---

Invoke the `working-backwards` skill.

Starts from a rough idea — the interview is the input. No PRODUCT / FEATURE / BROKE form to fill in first. S1 begins from an empty `app/`.

Follow the procedure in [`modules/s1-working-backwards/working-backwards-blueprint.md`](../../modules/s1-working-backwards/working-backwards-blueprint.md) verbatim. HALT before writing the spec until the problem statement is sharp, solution-free, every fuzzy word pinned, and the user has confirmed it — the confirmed problem is the one-way door everything downstream is generated from.
