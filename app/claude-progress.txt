2026-07-12T21:10:00Z  S1  problem confirmed (one-way door)  DEFENCE: after a wrong first pass that invented a Gmail/Calendar morning-brief app, the real exec-assistant logic (gks-exec-assistant templates) was copied onto main; the confirmed problem — a solo operator working the wrong things for lack of a held plan — is derived from the EA's own files, so the spec documents existing logic instead of inventing it.
2026-07-12T21:10:01Z  S1  spec.html landed  RATIONALE: every part is grounded in a template file (session ritual, task cap, 90s cockpit, brain-dump, prioritisation filter, PM-only, security) and the tech stack is pinned as markdown-read-by-an-LLM — no code/DB/API — so the eval grades the actual logic, not a fabricated app.
2026-07-12T21:10:02Z  S1  evals.md landed  RATIONALE: the EA's hard rules became binary checks (E2 cap, E7 no-execution, E8 no-invention, E11 instruction-in-file inert) gradeable from a session transcript + folder before/after.
2026-07-12T21:10:03Z  S1  CLAUDE.md landed  RATIONALE: the tenet "the logic already exists in the templates; never invent mechanics they don't have" is the durable guard against repeating the first pass's drift.
2026-07-12T21:10:04Z  S1  spec-summary.json landed  RATIONALE: downstream stages need the invariants and the markdown-only stack machine-readable.
2026-07-12T22:05:00Z  S1  ambiguity-audit.html landed  RATIONALE: the independent rescan of the grounded spec caught 5 two-way readings (route, specific sign-off, the untimeable 90-second rule, time envelopes, guardrail "shows") plus a blind spot — T6 had no eval row at all — proving even a template-grounded spec still hides ambiguity in its own vocabulary.
2026-07-12T22:05:01Z  S1  spec.html + evals.md re-pinned  RATIONALE: every flag closed at the spec layer (pins + the E6/E9 tightenings + new E13), so S3 grades countable bounds instead of taste — post-fix rescan shows zero open flags.
